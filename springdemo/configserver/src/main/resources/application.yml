
server:
  port: 8888
  address: 127.0.0.1
spring:
  cloud:
    config:
      server:
        native:
          search-locations: 'file:///G:/gitconfig'
          add-label-locations: false
  main:
    web-application-type: none
  profiles:
    active: 'native'
